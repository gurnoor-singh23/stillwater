export default function CTASection() {
  return (
    <section id="cta" className="mx-auto max-w-6xl px-6 py-24 text-center">
      <h2 className="font-display text-3xl md:text-5xl font-semibold text-paper leading-tight max-w-2xl mx-auto">Your next three minutes are worth this.</h2>
      <p className="mt-5 text-muted max-w-md mx-auto">No account required to try the first session. No card, no email, no pitch.</p>
      <button className="mt-8 rounded-full bg-signal px-8 py-4 text-sm font-semibold text-ink hover:scale-105 transition-transform">Start a session</button>
    </section>
  );
}


